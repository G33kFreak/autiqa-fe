# Copy to `.env` and adjust. Committed; no secrets here.
#
# API origin (no trailing slash). Used by browser `$fetch` and Nitro proxy routes.
# https://nuxt.com/docs/guide/going-further/runtime-config
NUXT_PUBLIC_API_BASE=http://localhost:3001
